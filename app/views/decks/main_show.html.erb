

<div class="search-bar">
  <form class="search-title-form">
    <input class="search-bar-n" type="text" placeholder="search titles">
    <input class="search-button-n" type="submit" value="search">
  </form>
</div>



<div id="welcome" class="welcome">
</div>


<script>

$.ajax({
  url: '/decks',
})
.done(function(data) {
  // debugger
  console.log(data);
  var context = { decks: data};
  var source = $("#deck-template").html();
  var template = Handlebars.compile(source);
  var html = template(context);
  $('#welcome').html(html)

})

</script>

